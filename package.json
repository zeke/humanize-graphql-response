{
  "name": "humanize-graphql-response",
  "version": "0.0.0-development",
  "description": "Clean up GraphQL repsonse objects by flattening nodes and edges into object arrays",
  "main": "index.js",
  "repository": "https://github.com/zeke/graphql-humanized-response",
  "author": "zeke",
  "license": "MIT",
  "devDependencies": {
    "jest": "^22.4.2",
    "standard": "^11.0.1",
    "semantic-release": "^15.1.3",
    "travis-deploy-once": "^4.4.1"
  },
  "scripts": {
    "test": "jest && standard --fix",
    "watch": "jest --watch --notify --notifyMode=change --coverage",
    "semantic-release": "semantic-release",
    "travis-deploy-once": "travis-deploy-once"
  },
  "standard": {
    "env": {
      "jest": true
    }
  }
}
